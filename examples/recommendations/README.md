# Recommendation Examples

This folder contains fictional example data for the recommendation module.

Files:

- `sample-input.json` — example input.
- `sample-output.json` — example output.

The examples use fictional identifiers only.

## Intended use

Use these files to understand how topic scores can become simple recommendation output.

The module is a transparent baseline for future development.
